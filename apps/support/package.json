{
  "name": "legacy-fusion-support",
  "version": "0.1.0",
  "description": "Legacy Fusion Support System — AI-driven support ticket management",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist",
    "preview": "vite preview",
    "deploy": "vite build --outDir dist && wrangler deploy",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}
